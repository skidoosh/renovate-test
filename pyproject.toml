[project]
name = "renovate-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Glyn Mooney", email = "749058+skidoosh@users.noreply.github.com" }
]
requires-python = ">=3.14.0"
dependencies = []

[project.scripts]
python = "python:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
exclude-newer="72 hours"
package = false

[dependency-groups]
dev = [
    "ruff==0.16.3",
    "ty==0.0.71",
]
